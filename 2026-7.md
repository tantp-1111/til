7/2
LINEのomniAuth振り返り
現在のgemを自前ストラテジに変更するにあたり、現在の使用を確認。
```
① ユーザーが「LINEでログイン」クリック
        │  GET /users/auth/line
        ▼
② omniauth-line gem が LINE の認可画面へリダイレクト（アプリを離脱）
        │
        ▼
③ ユーザーが LINE 上で「許可」→ LINE が認証
        │
        ▼
④ LINE がアプリの callback URL へ戻す
        │  GET /users/auth/line/callback
        ▼
⑤ gem がトークン交換し、ユーザー情報を request.env["omniauth.auth"] に詰める
        │
        ▼
⑥ ★ここから自分のコード★
   OmniauthCallbacksController#line → User.from_omniauth(auth)
        │
        ├─ 既存ユーザー？ → そのまま取得
        └─ 初回？          → 新規 User を作成
        │
        ▼
⑦ sign_in してダッシュボードへリダイレクト
```

